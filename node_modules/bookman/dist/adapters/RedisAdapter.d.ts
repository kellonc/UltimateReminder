import { WrappedNodeRedisClient } from "handy-redis";
import { BaseAdapter } from "./BaseAdapter";
import { BookmanOptions, LooseObject } from "../types";
import type { ClientOpts } from "redis";
export declare class RedisAdapter extends BaseAdapter {
    client: WrappedNodeRedisClient;
    constructor(options: BookmanOptions, redisOptions?: ClientOpts);
    set(value: string): Promise<LooseObject>;
    get(): Promise<LooseObject>;
    destroy(): Promise<boolean>;
    init(): void;
}
