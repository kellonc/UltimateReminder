import { BookmanOptions, LooseObject } from "../types";
export declare abstract class BaseAdapter {
    opts: BookmanOptions;
    constructor(opts: BookmanOptions);
    abstract set(value: unknown): Promise<LooseObject> | LooseObject;
    abstract get(): Promise<LooseObject> | LooseObject;
    abstract destroy(): Promise<boolean> | boolean;
    abstract init(): Promise<void> | void;
}
