import { BaseAdapter } from "./adapters/BaseAdapter";
import { LooseObject } from "./types";
export declare class Database {
    private adapter;
    private json;
    constructor(adapter?: BaseAdapter);
    private getDefaultData;
    set(name: string, value: unknown): Promise<unknown>;
    push(name: string, value: unknown): Promise<any[]>;
    pop(name: string): Promise<any>;
    shift(name: string): Promise<any>;
    unshift(name: string, value: unknown): Promise<any[]>;
    add(name: string, value: number): Promise<number>;
    subtract(name: string, value: number): Promise<number>;
    delete(name: string): Promise<LooseObject>;
    fetchAll: () => LooseObject;
    map: () => LooseObject;
    all: () => LooseObject;
    getAll: () => LooseObject;
    get: (name: string) => unknown;
    fetch: (name: string) => unknown;
    has: (name: string) => boolean;
    destroy(): Promise<boolean>;
}
